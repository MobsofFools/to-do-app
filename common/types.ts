export type TodoItem = {
    title:string,
    description:string,
    deadline?:string,
    location?:string,
    complete?:boolean
}
export type createdUser = {
    email:string,
    fname:string,
    lname:string
}